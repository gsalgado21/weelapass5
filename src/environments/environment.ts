// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

export let POSITION_INTERVAL = 20000;
export let GOOGLE_MAP_API_KEY = "AIzaSyBSm_YWaml70yFQz6PQDYDI0OsU4RBEs8A&language=pt-BR&region=BR";
export let GOOGLE_MAP_BASE_URL = "https://maps.googleapis.com/maps/api/";
export let HERE_MAP_API_KEY = "i8OLS-BmUP3tAG_nN7EYdz9H83Vt3kxm9AOHj9FxHKE";
/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
